# Context

This directory contains background context files that are loaded into Claude Code conversations to provide relevant information about the legacy codebase.

## Usage

Add markdown files here describing aspects of the legacy codebase (e.g. `codebase-overview.md`, `business-domain.md`). Reference them in CLAUDE.md or pass them directly in conversations.
